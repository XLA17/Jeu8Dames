<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <!-- TextView pour le message de victoire -->
    <TextView
        android:id="@+id/winMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Vous avez perdu !"
        android:textSize="24sp"
        android:layout_centerInParent="true"
        />

    <!-- Button pour recommencer le jeu -->
    <Button
        android:id="@+id/restartButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Recommencer"
        android:layout_below="@id/winMessage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp" />

    <!-- Button pour quitter le jeu -->
    <Button
        android:id="@+id/quitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quitter"
        android:layout_below="@id/restartButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:onClick="Leave"/>


</RelativeLayout>
